# Internal working document — the build prompts and working agreement.
# Stays on disk, never in the repo: this is process-meta about how the
# guide is made, not something the school publishes.
ews-claude-code-prompts.md

# Staging dir the Pages workflow builds into
_site/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Note: ews-decision-map.html IS committed on purpose — it is the
# deliverable people attach to email. It is generated by build.js from
# ews-decision-map.src.html + ews-pathways-data.js; never hand-edit it.
